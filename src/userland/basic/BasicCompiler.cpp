#include "BasicCompiler.h"

namespace PickShell {
    BasicCompileResult BasicCompiler::compile(const BasicProgram &program) const {
        (void) program;
        return {};
    }
} // namespace PickShell
