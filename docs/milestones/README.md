# Milestone detail pages

Each file is named with a **two-digit** milestone prefix so directory listings stay in numeric order (for example `07` before `11`).

The short overview and ordered index: **[`../milestones.md`](../milestones.md)**.
