# Installed default for gemini.service (/etc/gemini/daemon.conf).
# Precedence: CLI > environment > this file > defaults.
# Set pick_root / catalog_root / modules_root for a useful multi-user system.
socket=/run/gemini/gemini.sock
max_sessions=64
# pick_root=
# catalog_root=
# modules_root=
